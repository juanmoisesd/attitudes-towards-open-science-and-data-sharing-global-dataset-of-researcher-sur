# GitHub Pages configuration
# Since we are using a custom HTML dashboard, we disable Jekyll to ensure
# our index.html and visualization files are served exactly as they are.
# To disable Jekyll, we can create a .nojekyll file in the root.
